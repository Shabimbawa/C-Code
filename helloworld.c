#include <stdio.h>
#include <stdlib.h>
typedef struct{
    int top=-1;
    name customers[10];
}stack;

typedef char[10]

typedef name char[50];
int main(){
    stack cList;

 }


void push(stack * cList, name cust){

}
void pop(stack * cList){
    if(cList->top > -1){
        clist->top--;
    }
}